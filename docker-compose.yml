version: "3.9"

services:
  redis:
    image: redis:7
    ports:
      - "6379:6379"

  api:
    build: ./apps/api
    ports:
      - "3001:3001"
    environment:
      - REDIS_HOST=redis
      - PORT=3001
    depends_on:
      - redis

  web:
    build: ./apps/web
    ports:
      - "3000:3000"