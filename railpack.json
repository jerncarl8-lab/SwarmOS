{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "packages": {
    "python": "3.10"
  },
  "steps": {
    "install": {
      "commands": [
        "python -m pip install --upgrade pip setuptools wheel",
        "python -m pip install --no-cache-dir --target .python_packages fastapi==0.110.1 uvicorn==0.25.0 python-dotenv==1.2.2 asyncpg==0.30.0 bcrypt==4.1.3 pydantic==2.12.5 email-validator==2.3.0 resend==2.29.0"
      ]
    }
  },
  "deploy": {
    "startCommand": "PYTHONPATH=.python_packages python -m uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8001}"
  }
}
